info:
  name: DeletePrdouct
  type: http
  seq: 1

http:
  method: DELETE
  url: http://localhost:8080/api/v1/products/1
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
